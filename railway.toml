[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'if [ \"$SERVICE_TYPE\" = \"worker\" ]; then exec npm run worker; else exec npm run start; fi'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# schema version: 2
