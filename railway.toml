[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c '[ \"$SERVICE_TYPE\" = \"worker\" ] && npm run worker || npm run start'"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# schema version: 2
